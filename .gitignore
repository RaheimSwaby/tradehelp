node_modules/
out/
dist/
*.log
.DS_Store
.vscode/
/screenshots/
.claude/

# Local AI tooling and review artifacts — never publish
/.github/agents/
/.github/hooks/
/.github/skills/
/browser-screenshots/
*.odysseus-tmp
/marketing/

# Personal NinjaTrader strategy — never publish
/ninjatrader/

# Local demo/screenshot output — large binaries, not part of the app
/artifacts/

# Throwaway screenshots from verifying UI work, and exported share reports
.tmp-*.png
/TradeHelp-report-*.png

# Rendered promo video only. The marketing folder itself is tracked (ad pages and
# render scripts belong in the repo); it is the multi-MB output that must not be.
/marketing/**/*.mp4
/marketing/**/*.webm
.gstack/
